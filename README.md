# 🌾 Crop Prediction System using Machine Learning & Flask

A web-based **Crop Prediction System** that recommends the most suitable crop to grow based on soil and environmental parameters. The system uses a **machine learning model** integrated with a **Flask web application** to provide real-time predictions through a simple user interface.

---

## 📌 Features

- Predicts the best crop based on input parameters
- Uses a trained Machine Learning model
- Flask-based web application
- User-friendly HTML interface
- Fast and accurate predictions
- Easy to deploy and extend

---

## 🧠 Machine Learning Model

The model is trained on agricultural data containing the following parameters:

- **Nitrogen (N)**
- **Phosphorus (P)**
- **Potassium (K)**
- **Temperature (°C)**
- **Humidity (%)**
- **pH value**
- **Rainfall (mm)**

### Algorithms Used
- Random Forest Classifier *(recommended)*
- Decision Tree
- Naive Bayes *(optional)*
